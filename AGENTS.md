# TixRepo / ちけレポ 作業ルール

## 対象プロジェクト

- このリポジトリは TixRepo（ちけレポ）専用。
- 「サイトなしドットコム」は別案件なので、コード・データ・仕様を混ぜない。
- 既存の完成UIを尊重する。本番の乃木坂46アーティストページを表示基準とし、アーティストTOPにセトリを追加しない。

## 作業と引き渡し

- Codexは、可逆なローカル修正、テスト、ビルドを細かな確認待ちで止めず、自動で進める。
- 作業がひとまとまりになったら、確認可能な状態まで検証して自動でcommitする。commitはローカルの復元可能なセーブポイントとして扱う。
- commit後に変更内容と検証結果をユーザーへ報告する。
- push、GitHub Actions、本番Supabaseへの書き込み、Vercel deployは、報告後にユーザーの確認を得てから行う。
- 作業開始時に `git status` と直近履歴を確認し、ユーザーの既存変更を上書きしない。

## 必ず止めて確認する変更

- 新しい migration、本番DBスキーマ変更、または本番データの大量削除・全消去
- 公演、公式NEWS、投稿など既存データの一括上書き
- Secretsの削除、ローテーション、外部露出につながる操作
- ドメイン、料金、契約に関する変更
- 大規模な設計変更、サイト全体または完成済みUIの作り直し
- 人間の認証操作が必要な処理
- 既存ページを壊す重大な回帰
- Gitの過去履歴を消す操作、Git履歴の異常
- 本番NEWSの重複、本番書き込み失敗、対象サイトの大半の失敗

## 今後の優先順

1. lint・型チェック・テスト・buildをすべて通るローカル開発状態を保つ。
2. プロジェクト正典と実装・本番状況の食い違いをなくす。
3. 主要ページをローカルで画面確認する。
4. まとまった変更をcommitし、ユーザー確認後にpush・deployする。
5. 残りのアーティストの公式NEWS対応を少数ずつ追加する。
6. BE:FIRSTのGitHub Runner上のHTTP 403問題へ戻る。
7. ログイン以外の実装がすべて完了した後の最終工程として、Supabase Authによるログイン・ログアウト・マイページを実装する。それまでは認証パッケージの追加や認証コードの着手も行わない。

## 完成扱いにする既存機能

- 現在のヒーロー画像と画像フォールバックは完成扱いとし、追加作成や作り直しを行わない。
- 現在の座席予想と現地レポの詳細表示は完成扱いとし、新しい不具合がない限り変更しない。
- ログインは、ほかの未実装がなくなるまで完全に停止する。最終工程でSupabase Authを使い、まずGoogleログイン、ログアウト、簡単なマイページを作る。
- お気に入りアーティストと推し優先表示は、ログイン基盤の後に所有者RLSを備えたDB設計として追加する。

## 公式NEWSの現在方針

- BE:FIRSTのGitHub Runner上のHTTP 403調査は後回し。公式NEWS取得だけを一時無効化し、アーティストページや公演機能には影響させない。
- 本番成功済み12組を維持する。ローカルではONE OK ROCK、あいみょん、back number、Mrs. GREEN APPLEの4組を検証済みで、push・GitHub Actions・本番保存はユーザー確認後に行う。
- 未調査は残り75組。robots.txt禁止サイトを無理に回避せず、許可サイトを少数ずつ追加する。
- 1サイト・1記事の失敗で全体を止めず、失敗内容はレポートに残す。DBエラーは成功扱いにしない。
- 1回の新規処理上限は15件。上限超過分を `limit_deferred` として次回へ繰り越すのは意図した仕様。
- 再実行の重複判定は `(artist_slug, normalized_article_url)` を基準にする。繰越中の別記事追加と、同一記事の重複追加を混同しない。
- 公開側は `official_news_public` を使用し、`article_body` と `normalized_article_url` を公開しない。

## 当面の対象外

- BE:FIRSTの403調査
- NiziU、日向坂46、櫻坂46などrobots.txtがAI crawlerを禁止している公式NEWSサイトの回避実装
- migration baseline整理、032適用、crawl_runs用migration適用
- 重複公演のマージ、testデータ削除
- ヒーロー画像の追加作成、座席予想・現地レポ詳細UIの変更
- ログイン・マイページ実装と認証パッケージ追加（ログイン以外の全実装が完了した後に着手）
- 大規模な新機能追加、完成済みUIの作り直し、長大な再監査

## ローカル確認コマンド

```powershell
npm.cmd run typecheck:crawler
npm.cmd run test:crawler-safety
npm.cmd run test:artist-platform
npm.cmd run lint
npm.cmd run build
git status --short
```
