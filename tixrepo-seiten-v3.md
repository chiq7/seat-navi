# ちけレポ（TixRepo）プロジェクト正典 v3

> このドキュメントは「今、何が本物で、何が旧で、何がこれからか」を1枚に固定するための地図です。
> 新しいチャット・別のAIに作業を頼むときは、まずこれを最初に渡してください。
> **判断に迷ったら必ずこれを読み直してから進める。記憶や見た目だけで判断しない。**
>
> 最終更新：2026-06-30（夜・大規模整理後）

---

## 0. 大前提

- 公演なう（旧）を土台に、ちけレポ（新）へ作り直している。
- カラー：ちけレポ＝ピンク `#FF6B9D` ／ 公演なう＝teal・青緑（残骸の目印、見つけたら削除候補）。

---

## 1. 確定した「本物」一覧（2026-06-30 大規模整理後）

| 機能 | 本物URL | 状態 |
|---|---|---|
| アーティストページ | `/artists/[slug]` | ✅ 完成 |
| 現地レポ投稿 | `/report/live` | ✅ 完成・本番唯一の現地レポ入口。`/report`メニュー「現地レポを投稿」から接続済み。会場タイプ（ドーム/アリーナ・ホール・ライブハウス）に応じて座席エリア選択肢が自動で変わる |
| 現地レポ一覧（アーティスト単位） | `/artists/[slug]/after-reports` | ✅ 完成。日付・エリア・ブロックで絞り込み可 |
| 当落・座席報告 | `/report/ticket` | ✅ 完成。`/report`メニュー「当落・座席を報告」から接続済み |
| 予想図投稿 | `/events/[id]/fan-seat-prediction` | ✅ 完成 |
| セトリ投稿 | `/artists/[slug]/setlist` | ✅ リンク済み |

### 削除済み（旧バージョン・重複・未使用、2026-06-30整理で全削除）
- `/artist-page`・`/artists/[slug]/reports`・`/font-preview`（旧アーティストページ関連）
- `/report/live/detail`（DEMO固定データのみの孤立ページ）
- `/events/[id]/after-report`（現地レポ投稿の旧版。会場タイプ判定なしの固定5択だった。本物は`/report/live`に統一）
- 未使用コンポーネント計29ファイル（`src/components/artist/`全8件、`ArtistHeader.tsx`、`after-report/`系9件、`setlist/`2件、`event/`2件、`home/BottomNav.tsx`、`SeatReportForm`関連3件、`FanSeatPredictionsCarousel.tsx`）

**今後「これ本物？旧？」と思ったら、まずこの表を見る。表にないページは旧版・未使用の可能性が高いので、削除前にgrepで参照有無を確認してから判断する。**

---

## 2. 今日（2026-06-30）やったこと全体

### 午前〜午後：アーティストページ完成
1. 段階3：`LiveEffectsSection`をDBから集計接続（演出フラグ、点灯/消灯のみ。5段階メーターは現地レポ詳細側の機能）
2. 段階4：ファン予想図「いいね1位」を直近公演単位で表示
3. 旧ページ削除：`/artist-page`・`/artists/[slug]/reports`・`/font-preview`
4. 公演一覧カードを日毎→会場ごとにまとめ表示に変更
5. 今後公演ゼロ時の表示：ツアー名「公演発表待機中」、過去データでメトリクス表示
6. `/artists/[slug]/after-reports`一覧ページを新規作成

### 夕方：投稿機能の本番化
7. `after_reports`テーブルのCHECK制約修正（`center_stage`/`torokko`/`kyakukudari`を実フォーム値"なし/1〜5"に対応、マイグレーション025）
8. `/report/live`・`/report/ticket`のダミーevent_id（"tokyo-0712"等）を実DBの`events`データ取得に差し替え
9. `seat_area_type`の日本語→DB値変換マッピング追加（`toSeatAreaType`）
10. `/report/live`に写真アップロード機能を実装（`after-report-photos`バケット・複数枚・プレビュー・削除）

### 夜：重複ページの発覚と大規模整理
11. **重要な発見**：`/report/live`と`/events/[id]/after-report`という2つの現地レポフォームが並行して存在していた。`/report/live`が正典上の本物だが、`/report`メニューは旧版の`/events/[id]/after-report`を向いていた
12. 実機確認の結果、`/report/live`の会場タイプ自動判定（ドーム/アリーナ・ホール・ライブハウス）は正常動作と確認
13. プロジェクト全体を棚卸しし、未使用コンポーネント29ファイル・孤立ページ2件・重複ページ1件を削除
14. `/report`メニューを修正：「現地レポを投稿」→`/report/live`、「当落・座席を報告」→`/report/ticket`に接続
15. ダミーデータ削除：`/report`ページの"NiziU"固定表示、`HotReportsSection`・`RealtimeFeedSection`の偽データ配列
16. BottomNavの死リンク修正（`afterHref`を`/report/live`に統一）
17. DB整理（マイグレーション026）：コード参照ゼロの8テーブルを`DROP TABLE`、実データを持つ6テーブル（`events`以外）を`TRUNCATE`して全てのテスト投稿データをクリア

---

## 3. 既知の不具合・未解決事項

### 3-1. ホール/ライブハウス系の座席エリアDB保存（仕様として保留中）
`/report/live`はホール（1階席/2階席以上/バルコニー/その他）・ライブハウス（指定席/スタンディング/整理番号/その他）の選択肢を持つが、DB（`seat_area_type`）のCHECK制約は`arena`/`stand_1f`/`stand_2f`/`stand_3f_or_higher`/`other_unknown`の5種類のみ。**現状の方針：ドーム/アリーナ系だけ正確に保存できれば十分とし、ホール/ライブハウス系の精緻化は後回し。** 対応するときは、DB制約自体を増やす方向で検討する（マッピングで意味をねじ曲げない）。

### 3-2. 会場タイプの仕訳元データがない
`events`テーブルには会場タイプ情報がなく、`/report/live`は`venue`名の正規表現マッチでその場判定している（ドーム|アリーナ|スタジアム → arena_dome_stadium、ホール|劇場 → hall_theater、Zepp等 → livehouse_other、それ以外はarena_dome_stadiumにフォールバック）。曖昧な会場名だと誤判定の可能性が残る。本格的にやるなら「会場リストをスクレイピング→機械的一次仕訳→AIチェック→人手確認」のフローが過去に検討されていた（未着手）。

### 3-3. 完了画面の導線不足
`/report/live`・`/report/ticket`の投稿完了画面に、トップページや投稿先アーティストページへの導線がない（「もう1件投稿する」「別の公演を投稿する」のみ、ヘッダーの「戻る」は`/report`に戻るのみ）。

### 3-4. ログイン／マイページ
認証ロジック未実装（Supabase Auth未使用）。

### 3-5. アーティストとDBの紐付けがコード依存
`artists`テーブルは存在しない。アーティスト情報は`src/lib/artists.ts`のハードコード定数で管理しており、`events`との紐付けは`title`のキーワード部分一致で行っている。将来的にアーティスト数が増えると曖昧一致のリスクがある。

---

## 4. 次にやるべきこと（優先順位）

1. **完了画面の導線追加**：投稿後にトップ/アーティストページへ戻れるように
2. ログイン／マイページ実装
3. 会場タイプ仕訳（スクレイピング＋人手確認のフロー）
4. ホール/ライブハウス系座席エリアのDB制約拡張（必要になったタイミングで）

---

## 5. 運用ルール（今回の反省を踏まえて追加）

- **「これは本物？旧？」の判断に迷ったら、必ずこの正典を読み返してから答える。記憶やコードの見た目の完成度だけで判断しない。**
- **複数の似たページが見つかったら、まず実機（ブラウザ）でURLを確認してから「どっちが本物か」を判断する。コードの行数や機能の多さだけで「こっちの方が良さそう」と判断しない。**
- 大きな作業を始める前に、正典の「次にやるべきこと」と整合しているか確認する。
- 作業が一区切りついたら、その都度この正典のセクション2・3を更新し、新しいバージョンとして保存する。
- DBやファイルの削除など不可逆な操作は、必ず事前にTakaの明示的な確認を取ってから実行する。
